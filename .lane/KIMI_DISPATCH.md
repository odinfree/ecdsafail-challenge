# Kimi K3 tape-transducer dispatch

You own only this isolated worktree and branch. Read
`/Users/olifreuler/ecdsa-ops/skills/burn-the-house-down/SKILL.md` completely
before acting, then inspect the exact promoted source and adjacent accepted
history.

## Objective

From promoted source `2c79d2f4ef1f6fdc6f75024ef4ae50c6506e2208`
(measured Q1274, rounded T916526, score 1167654124, full 9,024-shot `0/0/0`),
find a structural route that lowers peak Q. Prioritize the `pp_div_replay`
tape/boundary-carry owner and an in-place retained-word transducer that never
materializes raw `(u,v)`.

Use Burn the House Down methodology: overturn assumptions, identify exact peak
co-binders, price a bounded saddle before implementation, kill impossible
families early, compose only measured wins, and grind last.

## Hard gates

1. Before any semantic source edit, create and commit a predeclaration under
   `.lane/` naming one bounded architecture family, exact source/tree
   identities, peak/Q hypothesis, score ceilings, tests, stop rules, and
   excluded actions.
2. Reopen `ecdsafail benchmark` before pricing. Current arithmetic references:
   at Q1273 strict rounded T must be at most 917245; at Q1272 at most 917967.
   Recompute from live and do not trust copied values.
3. Instrument or inspect the actual Q1274 peak owners before claiming a cut.
   Existing evidence commit `093d85d` may be inspected as adjacent private
   history, but this lane starts from `2c79d2f` and must reproduce its own facts.
4. Try exactly one bounded structural family. Prefer a proof-quality lower
   bound and early KILL over broad speculative edits. If it survives pricing,
   implement it default-off first, run focused miters, selftests, and peak/T
   measurements, then bake only after the gates pass.
5. A candidate needs a clean rebuild, exact Q, measured T below the live
   ceiling, complete value/phase/ancilla selfchecks, and unchanged evaluator
   semantics. At most one inherited-nonce full 9,024-shot diagnostic is allowed
   after all cheap gates pass. A dirty draw is evidence, never a submission.
6. No nonce hunt, provider or cloud compute, `ecdsafail` submission, public note,
   external message, API-key access, or edits outside this worktree. Do not
   touch other worktrees.
7. Do not commit `ops.bin`, `results.tsv` changes, `target`, logs, caches, helper
   binaries, generated artifacts, or temporary evaluators. Preserve unrelated
   tracked history.
8. Critique, fix, and verify every deliverable. Record exact dead ends and the
   next binder in `.lane/KIMI_TAPE_TRANSDUCER.md`. Commit and push durable
   predeclaration and terminal evidence or source checkpoints to the `odinfree`
   remote.

End with exact commits, Q/T/score or lower bound, tests, verdict, and next
action. Model context: Kimi Code k3, high effort. Work autonomously and do not
ask for permission.
