# E-006 final-y boundary-borrow composition

Predeclared on 2026-08-23 before source implementation or candidate
measurement. This starts from clean pushed commit
`4e33a0c22e8bb3775fee7ab23ab1593ed9f06c6d` and the held stream
`SUB4_PP_REPLAY_FOLD_WINDOW=55 SUB4_PP_MUL_PLUS2F_ALIAS=1`.

## Overturn and exact state

E-004 disproved the claim that a wider low reduction window preserves the
missing state. A 53-bit window left `+2^53`; widening it to 54 merely moved the
error to `+2^54`. The final modular subtraction is conditionally subtracting
`F = 2^32 + 977` from a 256-bit wrapped difference. Its low-window operation
must export the borrow crossing bit 53, not move the truncation boundary.

The smallest semantic state is one clean qubit `boundary_borrow`:

1. Prepare the 53-bit constant `F` once.
2. Perform the existing conditional low subtraction as complemented addition,
   but use an exact controlled vented Cuccaro adder with a carry-out. That
   carry-out is exactly `borrow = condition AND (low_before < F)`.
3. Controlled on `borrow`, decrement the untouched high suffix `y[53..256]`
   using complement / exact controlled increment / complement.
4. Copy the corrected low 53 bits, conditionally add `F` with carry-out into
   the same borrow qubit to erase it, conditionally subtract `F` from the copy,
   uncopy, unload `F`, and free all workspace.

The borrow qubit is the only state crossing the low/high boundary. Constant,
prefix, and copy registers are transient reversible workspace. The opt-in is
named `TLM_FINAL_Y_SUB_BOUNDARY_CARRY53=1` and applies only to
`tlm_coord_y_sub_final`; every other subtraction and all defaults remain on
the exact prior path.

## Static price and co-binders

The SHA-guarded held profile measures final-y peak Q1026. The existing global
Q1272 co-binders are divide replay, product-register square, multiply replay,
and multiply walkback; all occur before this opt-in final shell.

For width 53, the conservative transient-qubit bounds are:

```text
capture:   F53 + carry-chain53 + boundary1                 = 107
decrement: F53 + boundary1 + Khattar/Gidney prefix5        =  59
erase:     F53 + low-copy53 + carry-chain53 + boundary1    = 160
```

Even pessimistically adding the largest block to the already measured phase
peak gives Q1186, 86 below Q1272. The composition therefore cannot create a new
global co-binder if every workspace qubit is returned.

Each exact controlled vented low add emits at most 107, 107, and 105 CCX for
capture, borrow erasure, and copy restoration. A 203-bit controlled decrement
uses at most 611 CCX under the existing prefix construction. The conservative
incremental ceiling is therefore 930 T per shot. Applied on top of the held
exact average T916182.965865331, this bounds rounded full T at 917113, leaving
4026 below the hard ceiling 921139. This is an upper bound; actual executed T
and allocator Q must still be measured.

## Frozen falsifier and gates

The first and only targeted corpus is the original E-004 pair, not rebound
shot numbers from a changed operation stream:

- control: nonce `444000000032`, original shot 7996 target/offset;
- fault: nonce `444000000032`, original shot 7997 target/offset.

The fixed-input probe must preserve the five balanced phase cuts from E-004.
Both lanes must be exact after add3x, square, multiply restore, final y-sub,
rsub, and tail, with phase and non-register dirty state zero. The fault lane
must remove the exact `+2^53` delta while the control stays unchanged.

Only this one micro-composition may be implemented. A fixed-shot failure,
workspace leak, Q above 1272, rounded T above 921139, focused primitive failure,
or protected-hash drift stops the turn immediately. No alternate erasure,
suffix width, or borrow primitive may be tried adaptively.

If and only if the fixed pair passes, measure the exact same-seed 64-lane
profile and focused subtraction miter. Only if those pass may an inherited full
evaluation and the already frozen H64 block run. No new nonce, scan, provider,
spend, or submission action is authorized.

Protected reproduction remains:

- default: 12,901,167 ops, SHA
  `ecc3d9f0bb1dd4e68e6d337e39c4cdb66cfbfe83928a81fec28e22797fca4cfd`;
- promoted fixture: SHA
  `d9737f5154cf1159d1115f5057dcc8019b42984507f4e0cbeacd80edaee0b124`;
- held opt-in: 12,939,336 ops, SHA
  `d4ecab6dd80d044d0a28ae456a9106992d2b3aae070d80cfa0dde3d24400f920`.
