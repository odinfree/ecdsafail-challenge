# Claude Fable 5 — direct Q1272 replay-ladder descent

You own only this isolated worktree and branch. Read
`/Users/olifreuler/ecdsa-ops/skills/burn-the-house-down/SKILL.md` completely
before acting. Work autonomously and do not ask for permission.

## Frozen base and inherited evidence

Start from exact promoted source
`2c79d2f4ef1f6fdc6f75024ef4ae50c6506e2208` (Q1274, rounded T916526,
score 1167654124, unchanged full 9,024-shot `0/0/0`). Reopen the live
benchmark and submissions before pricing; recompute every ceiling from the
current live score.

The sibling Fable lane at commit `c70526d` corrected the live-source peak
census to four tied Q1274 binders and measured the first allocation
contraction:

- `SUB4_PP_PEAK=1273 SUB4_SQUARE_LADDER=243` gives trusted Q1273;
- paired 64-lane executed-T delta is +632 to +678 over the live parent;
- the one inherited full diagnostic is `12/12/0` and therefore hunt-only;
- all four binders remain budget-filled carry ladders;
- no semantic source edit was needed for the diagnostic configuration.

Do not repeat Q1273. This lane tests the next direct contraction while keeping
the live source's traversal depths and width schedule unchanged.

## One bounded family

Set only `SUB4_PP_PEAK=1272 SUB4_SQUARE_LADDER=242` on exact `2c79d2f`.
Keep divide/multiply rounds, R1/R2, width schedule, replay fold, arithmetic,
and evaluator semantics unchanged. The hypothesis is that the same exact
carry-layout repartition removes two simultaneous ladder wires across all four
co-binders, reaches trusted Q1272, and costs no more than the Q1272 strict
Toffoli headroom.

This is deliberately distinct from the active Q1272 selector pilot, whose
circuit source is `14608572`, operation SHA `678c149f...22f0`, and whose
696/696 traversal plus fused-fold selector architecture has screened millions
of nonces without a survivor. A surviving direct-ladder result must report its
own source/ops/checkpoint identity and must not reuse that pilot's model or
fault-density claims.

## Gates

1. Before any armed Q1272 measurement or semantic source edit, write and push
   one predeclaration under `.lane/` binding exact source/tree, live score,
   strict Q1272 rounded-T ceiling, expected four-way peak cut, tests, stop
   rules, and exclusions.
2. Reproduce the exact parent control and the sibling Q1273 configuration.
   Verify both peak-owner censuses and byte identities before Q1272.
3. Run the Q1272 configuration through builder and trusted Q checks, complete
   phase-profile census, square miter, full 64-lane point-add composition, and
   two paired deterministic 64-lane T measurements. Require classical,
   phase, and ancilla all clean in focused gates.
4. At Q1272 the live-source headroom is thin: the prior two measured one-step
   prices suggest roughly +1264 to +1356 T against +1441. If rounded T exceeds
   the freshly computed strict ceiling, allow exactly one predeclared local
   R1/R2 rebalance only when the profile names the paid cliff. Otherwise kill
   the family. No broad sweep.
5. If and only if Q, T, focused correctness, and determinism all pass, run one
   unchanged inherited-nonce full 9,024-shot diagnostic. Record exact
   classical/phase/ancilla and exact average T. A dirty draw is structural
   evidence, never a candidate or submission.
6. If the family survives, state the smallest exact classical-plus-phase
   predictor port/qualification route and whether the existing Q1273 wrapped
   model can be adapted without changing its semantic recurrence. Do not
   claim reuse from labels alone.
7. Critique, fix, and verify the result. Commit and push durable predeclaration
   and terminal evidence. If source defaults are changed, keep the route
   default-off until all gates pass.

## Hard exclusions

No provider or cloud compute, nonce hunt, range declaration, submission,
public note, API-key access, or external message. Do not edit other worktrees.
Do not commit `ops.bin`, `results.tsv`, score files, binaries, target trees,
logs, caches, temporary evaluators, or generated artifacts.

End with exact commits, Q/T/projected score, operation identities, test
receipts, full diagnostic if admitted, verdict, predictor handoff, and next
binder. Model context: Claude Fable 5, high effort.
