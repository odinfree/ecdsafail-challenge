# Burn-the-House-Down — direct live-source Q1271 ladder saddle

Own only this isolated worktree and branch. Read
`/Users/olifreuler/ecdsa-ops/skills/burn-the-house-down/SKILL.md` completely
before acting. Work autonomously and preserve the promoted source by default.

## Objective

From exact promoted source
`2c79d2f4ef1f6fdc6f75024ef4ae50c6506e2208` (Q1274, rounded T916526,
score1167654124, full 9,024-shot `0/0/0`), test whether the already-measured
four-way carry-ladder plateau can descend directly to Q1271 without changing
the live source's 698/696 traversal depths, width schedule, R1/R2, replay fold,
or arithmetic semantics.

Sibling evidence `c70526d` measured
`SUB4_PP_PEAK=1273 SUB4_SQUARE_LADDER=243` at trusted Q1273 and +632 to
+678 executed T. A separate Fable lane is measuring Q1272. This lane is the
next independent bounded saddle: Q1271 via
`SUB4_PP_PEAK=1271 SUB4_SQUARE_LADDER=241`.

## Frozen economics

Reopen `ecdsafail benchmark` and submissions before any candidate run. Against
score1167654124, the copied Q1271 ceiling is rounded T918689 and the live
parent headroom is2163; recompute it from fresh state. Three one-wire prices
near +632..+678 imply a narrow projected margin, so exact paired measurements
control the decision.

## Gates

1. Before any Q1271 armed measurement or semantic edit, commit and push a
   predeclaration naming the exact source/tree, live score, four co-binders,
   Q/T hypothesis, deterministic corpora, stop rules, and exclusions.
2. Reproduce the parent and sibling Q1273 controls. Then run a prerequisite
   Q1272 row (`PEAK=1272`, `SQUARE_LADDER=242`) only to establish whether the
   allocator still follows the four-way plateau; do not duplicate a hunt or
   predictor lane.
3. Admit Q1271 only if builder Q and trusted max-referenced-id Q both equal
   1271 and every phase is <=1271. Name any exposed floor precisely.
4. Require square and full point-add focused miters, 64-lane composition
   value/phase/ancilla `0/0/0`, opt-out identity, and two paired deterministic
   T deltas. Rounded T must be <= the freshly computed strict Q1271 ceiling.
5. Allow exactly one R1/R2 rebalance only if a measured cliff narrowly breaks
   the T ceiling and the profile identifies the responsible rounds. No broad
   sweep, no round/depth/width/fold change, and no repair imported from another
   architecture.
6. If all cheap gates pass, allow one inherited-nonce unchanged full
   9,024-shot diagnostic. Record exact classical/phase/ancilla and average T.
   Dirty means `HOLD-HUNT`, never clean or submission-ready.
7. Record whether the Q1273 wrapped classical and conditional-phase recurrence
   can be ported semantically, while treating operation/checkpoint hashes and
   fixture qualification as new. No model reuse by configuration label.
8. Critique, fix, verify, and push durable terminal evidence. Default-off
   diagnostic flags are sufficient; do not bake defaults unless every gate
   passes and a source checkpoint genuinely helps the next lane.

## Exclusions

No provider/cloud compute, nonce/range hunt, submission, public note, API-key
access, external message, or edits outside this worktree. Never commit
`ops.bin`, `results.tsv`, scores, binaries, `target`, logs, caches, helpers,
temporary evaluators, or generated artifacts.

End with commits, exact Q/T/score and margins, operation identities, gate
receipts, diagnostic result if admitted, verdict, predictor handoff, and the
next exposed binder.
