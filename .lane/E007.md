# E-007 post-low boundary predicate

Predeclared on 2026-08-23 before source implementation or any measurement in
this lane. This experiment starts from clean pushed commit
`632253d62f8c3c2c3bfee431583e934b206faa5d` and the held stream
`SUB4_PP_REPLAY_FOLD_WINDOW=55 SUB4_PP_MUL_PLUS2F_ALIAS=1`. It consumes the
independently developed alternative from the balanced-peak audit, not the
killed E-006 capture/restore erasure path.

## One admitted representation

The existing low-window subtraction leaves the corrected low result
`z_low = low_before - anc*f (mod 2^53)` live, where `f = 2^32 + 977`. Its
missing borrow is therefore recoverable without retaining a measured carry:

```text
b = anc && (z_low >= 2^53 - f)
  = anc && (z_low >= 0x1ffffefffffc2f)
```

The sole admitted opt-in is `TLM_FINAL_Y_SUB_BOUNDARY_CARRY=1`, active only in
phase `tlm_coord_y_sub_final`:

1. Toggle one clean qubit `b` from `anc` and the post-low 53-bit result using
   the exact disjoint threshold DNF.
2. Controlled on `b`, decrement all 203 high bits with
   `X(high); controlled-CINC(high, b); X(high)`.
3. Recompute the identical threshold predicate from the unchanged low result
   to return `b` to zero, then free it.

No low-window widening, captured Cuccaro carry, copy/restore erasure, alternate
threshold, suffix truncation, or adaptive phase repair is authorized. In
particular, E-006's phase-dirty erasure construction remains removed.

## Static price and co-binders

The final-y shell peaks at Q1026 on the protected held profile. Its only new
semantic state is `b`; the exact MCX decomposition and the controlled increment
use transient clean workspace which is freed locally. The four global Q1272
co-binders remain divide replay, product-register square, multiply replay, and
multiply walkback, all before this final shell.

The handed prototype predicts 12,943,345 emitted operations, candidate SHA
`4618d4af86c23c06cc72fef26f8a8e5986c7a9de5f804891541a8ce6eb189f37`,
same-seed diagnostic T918043.41, final-y peak Q1026, and global Q1272. These are
external expectations, not measurements by this lane. The measured gate is
Q1272 and rounded full T at most 921139. Any additional co-binder or threshold
breach kills the experiment.

## Frozen focused corpus and gate order

The fixed-input corpus is the original E-004 pair for nonce `444000000032`,
not rebound lanes from the changed operation stream:

- control: original shot 7996 target/offset;
- fault: original shot 7997 target/offset.

The probe preserves the E-004 balanced phase cuts. Both inputs must be exact
after add3x, square, multiply restore, final y-sub, rsub, and tail; phase and
all non-register qubits must be zero. The fault lane's prior exact `+2^53`
delta must disappear while the control remains unchanged.

The adversarial primitive miter is frozen at these 13 `(anc, value)` states:

```text
(0, 0), (0, 2^256-1), (0, f-1),
(1, 0), (1, 1), (1, f-1), (1, f), (1, f+1),
(1, 2^53-1), (1, 2^53), (1, 2^54),
(1, 2^100), (1, 2^256-1)
```

Every lane must produce `value - anc*f (mod 2^256)`, preserve `anc`, leave
phase zero, and return every scratch qubit to zero. This explicitly exercises
the threshold edge, ordinary borrow, long high-suffix ripple, and wraparound.

The frozen order is:

1. inspect the balanced-peak source/evidence commit independently;
2. verify the three opt-out protected streams exactly;
3. run the pinned 7996/7997 fixed-input probe;
4. run the complete 13-lane primitive value/phase/ancilla miter;
5. measure protected hashes, global Q/co-binders, and the same-seed 64-lane
   diagnostic under `PP_PROFILE_SEED=hard-fold-55`;
6. only if every earlier gate passes, run the inherited nonce
   `251000962439` through the unchanged full 9024-shot evaluator;
7. only if that passes, run the already frozen H64 block
   `444000000000..444000000063`, all 64 complete 9024-shot evaluations.

The inherited and H64 gates forbid a new classical mismatch relative to the
held stream's exact SHA-bound mismatch sets; candidate sets must be subsets of
the corresponding held sets. Phase and ancilla are reported honestly and
ancilla must remain zero. H64 is run in full even if an early nonce fails, but
no subsequent experiment follows a failed gate.

Protected reproduction remains:

- default: 12,901,167 ops, SHA
  `ecc3d9f0bb1dd4e68e6d337e39c4cdb66cfbfe83928a81fec28e22797fca4cfd`;
- promoted fixture: SHA
  `d9737f5154cf1159d1115f5057dcc8019b42984507f4e0cbeacd80edaee0b124`;
- held opt-in: 12,939,336 ops, SHA
  `d4ecab6dd80d044d0a28ae456a9106992d2b3aae070d80cfa0dde3d24400f920`.

Any protected drift, fixed-shot error, primitive value/phase/ancilla failure,
Q above 1272, rounded T above 921139, or new full-evaluator mismatch stops the
turn. No nonce hunt, provider action, spend, submission, or generated-artifact
commit is authorized.
