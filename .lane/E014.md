# E-014 fully coherent high200 carry propagation

Frozen on 2026-08-23 before E-014 source edits, temporary harnesses, candidate
operation output, or measurement. Base is clean pushed E-013 terminal commit
`9f3c6fb` on `research/q1272-call9-phase-clean`.

## Sole admitted repair

Recreate E-011 exactly through the clean low56 ladder apex:
`b = carry55 AND NOT sum55`. Keep `b` as a live coherent control, but replace
only E-011's vented Khattar-Gidney high200 controlled increment with the same
increment's fully coherent forward plus inverse prefix network. Then recompute
and uncompute `b` from the still-live apex and execute the inherited low-ladder
cleanup and inherited downstream overflow erasure in their original order.

The coherent increment path may contain X/CX/CCX only. It may not emit HMR,
reset, or measurement-conditioned phase operations; no width, predicate,
direction, comparator, erasure order, or second arithmetic repair may change.
The opt-in is `TLM_SQUARE_C_HALF_BOUNDARY_COHERENT=1`; every non-call-9 path
and protected default must remain operation-byte-identical.

## Static price and identity

- inherited E-007: 12,943,345 operations /
  `4618d4af86c23c06cc72fef26f8a8e5986c7a9de5f804891541a8ce6eb189f37`;
- inherited exact H64 T/Q: 918113.406063 / Q1272;
- live rounded-T ceiling: 919105;
- E-011 focused increment price: `+606.297` executed T;
- remaining allowance over E-011: about 385.7 T;
- restoring the KG reverse pass is expected to add about 198 Toffolis over
  E-011, inside that allowance;
- no additional coherent qubit is expected, so Q1272 must remain fixed;
- repaired miter seed provenance remains
  `b"e011-call9-boundary-miter"` and is not claimed as E-011 seed continuity.

The experiment dies before any corpus if measured incremental T over E-011 is
above 385.7, peak exceeds Q1272, the high200 repair emits any HMR/R, or any
frozen classical, phase, or ancilla gate fails.

## Frozen gates

1. Run the exact repaired-seed eight-lane miter. Require all eight complete
   values exact, phase mask zero, and every non-register qubit zero. Record
   Q/emitted T and assert the coherent high200 substream contains no HMR/R.
2. Run unchanged 64-lane `product-register-square-inputs`; require exact value,
   phase, and ancilla for every lane.
3. Build E-007 plus E-014; require Q1272, protected opt-out identities, and
   frozen same-seed diagnostic rounded T at most 919105 with no channel
   regression.

Stop at the first failure. No full 9,024-shot evaluation, H64, scan, hunt,
provider/remote action, spend, submission, adaptive repair, or generated
artifact commit is authorized. If E-014 fails, close this call-9 micro-repair
family rather than adding another local variant.

## Terminal result

The first checked build stopped before simulator output because the otherwise
coherent inverse freed five proven-zero prefix ancillas through reset `R`.
Replacing those frees with zero-qubit loans enforced the already-declared
no-HMR/R coherent implementation; it did not change the arithmetic or add an
alternative repair.

The frozen miter then preserved all eight exact values and every input, with
dirty mask zero and Q515. The high200 coherent substream passed its explicit
no-HMR/no-R assertion. Focused emitted T was 821 versus E-011's 817: only
`+4`, not the static `~198` estimate, because this logarithmic prefix instance
had four vented reverse ANDs. The phase mask nevertheless remained `0x30`.

The first gate failed, so square64, composition, and Q/T were not run. All
coherent-CINC plumbing, source changes, and the temporary miter were removed;
protected source/results hashes reproduce exactly.

Terminal verdict: `VALUE_EXACT; DIRTY_ZERO; COHERENT_PATH; PHASE_KILL;
SOURCE_REMOVED; FAMILY_CLOSED`. No further local call-9 micro-repair is
admitted from this lineage. No hunt, provider, remote, spend, or submission
action ran.
