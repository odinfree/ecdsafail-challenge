# Kimi tape re-descent on live 087cafa

Work only in this isolated worktree. The exact base is promoted source
`087cafaef46a4e339644a6191ff2df2e7031cb80`, measured Q1275, rounded T918972,
score 1171689300, full 9024-shot `0/0/0`.

Read `/Users/olifreuler/ecdsa-ops/skills/burn-the-house-down/SKILL.md` completely
before acting. Apply its overturn, re-descent, bounded-saddle, composition, and
grind-last method. This lane is independent from the carry-source composition
being tested elsewhere.

Objective: find a structural tape/transcript lifetime, packing, streaming,
recompute, or passenger-removal change that lowers the live Q1275 plateau while
retaining a score path. Focus on the ping-pong walk/replay tape and its exact
co-binders. Do not spend the turn on another ordinary nonce search or a duplicate
square-ladder notch.

Requirements:

- Force clean builds. Never trust a cached `ops.bin` or `score.json`.
- Measure the complete peak-owner set, not only the first peak line.
- Keep a co-binder ledger and price every local cut in Toffoli per qubit.
- Treat a structural width edit as binary. Add focused reversible self-tests,
  then require the unchanged full 9024-shot evaluator to return classical,
  phase, and ancilla `0/0/0` before calling it huntable.
- Q1274 must have rounded T at most 919693 to beat the live score. A lower-Q
  route must be score-gated against 1171689300 using exact integer arithmetic.
- No external compute, provider changes, nonce fleet, submission, or push.
- Do not modify files outside this worktree.
- Do not commit generated artifacts, logs, `ops.bin`, `score.json`, or helper
  binaries.
- Maintain `.lane/STATE.md` and `.lane/EXPERIMENTS.md` with exact commands,
  hashes, measurements, negatives, and the next falsifier.
- If a source change survives its gates, commit source plus durable lane notes
  with a clear message. If it fails, preserve the negative and pivot to the next
  structural hypothesis rather than grinding parameters.

End with a concise result containing current branch/commit/status, measured
Q/T/score, validation triple, source diff summary, and next action.
