# Q1270 native recurrence derivation receipt

Date: 2026-08-23. Decision: `PASS / TARGET-NATIVE INPUTS SEALED`.

This gate used only circuit source `90770b10664fc89065b1d05ac792370efed4c629`
and an independently rebuilt target binary. It did not read or copy a Q1271
model, table, checkpoint, operation stream, schedule, mask, constant, or output.

## Fail-closed correction

The first invocation stopped before launching the diagnostic binary or writing
an output because four lifecycle anchors named environment-variable fragments
that also occur in source self-tests. Commit
`ebc88ddaada634187e4aad8646fbba2d09e185ae` narrowed those anchors to the
unique production expressions. The reviewed runner has SHA-256
`c4fbe975d4f94a6f33acb875cd956039c4bf4e9d27fdcdf99f8505bbb03d387c`.
No Python cache, operation artifact, binary, or log is tracked.

## Frozen inputs

- source / tree: `90770b10664fc89065b1d05ac792370efed4c629` /
  `944593de97d412f8b4c7242f7d0aff98002c0d04`;
- `pingpong_div.rs` SHA-256:
  `9b1e5e8540a01584e1238c27328481ba67db3b167aa199db4113e846f1eb6295`;
- square product-register SHA-256:
  `864d31454c5279632652cf48aeda038d481a504b09e4f7a8c0f266e10a24b81c`;
- independently rebuilt diagnostic binary SHA-256:
  `1011f82a46a034fff7c82307354e83b1d4a24d2c3c740bc3caa3449f93f555a4`.

The binary identity is the byte-exact identity already independently rebuilt
and sealed from the target source. The invocation environment contained only
the fixed `PATH` and `SUB4_DUMP_WSCHED=1`; it emitted a framed zero-operation
diagnostic, not a candidate circuit.

## Exact derivation

The production source and diagnostic bind:

- 704 base rounds and separate 696-round divide/multiply selection at lines
  6, 13, and 28;
- the 700-entry embedded width table and runtime mapping at lines 435-512;
- `floor(round*703/695)` rescaling with the repair branch disabled by literal
  `false`;
- replay chunk 96, compare 20, replay fold 54, endpoint fold 20, and flag
  compare 22;
- default plan boundaries 340 and 628 and the production peak-selector
  mechanism at lines 1287-1289;
- production selector/sign-XOR lifecycle helpers at lines 557 and 565,
  target0/sign alias at line 1901, and doubled-out eviction at line 1971;
- the square-ladder control at product-register line 41.

The executable probe returned all 700 contiguous rows. Every base and rescaled
width equaled the value independently parsed from the frozen source table.
Both sequences contain 252 distinct widths and are non-increasing. Selected
rescaled rows are `0:259`, `1:258`, `339:145`, `340:145`, `627:34`,
`628:33`, `695:8`, and `699:8`. The active divide and multiply schedules each
contain exactly 696 rows.

The diagnostic ran with only `SUB4_DUMP_WSCHED=1`. It did not activate or
probe the target peaks, square ladder, or four lifecycle controls: their source
defaults are different. Those eight values below are frozen configuration
inputs from `PREDECL.md` SHA-256 `d2bced1e...7df9`, corroborated by the
byte-exact target rebuild receipt SHA-256 `7a7f8257...95e9` and operation
stream `ec4fadc0...eb63a`. They are not width-probe observations:

- divide/multiply rounds: `696/696`;
- divide/multiply peaks: `1270/1271`;
- plan boundaries: `340/628`;
- replay chunk/compare/fold: `96/20/54`;
- endpoint fold / flag compare: `20/22`;
- square ladder: `240`;
- selector eviction, doubled-out eviction, target0/sign alias, and routed
  sign-XOR/add-out checkpoint: enabled.

The native executable must require this explicit configuration identity and
must fail closed if built or invoked against source defaults. A self-test that
passes with the target configuration omitted is evidence for the wrong stream.

## External artifact receipt

All generated files remain under
`/Users/olifreuler/ecdsa-ops/q1270-routed-combined-ec4fadc0/model-derivation-4aaf5b8`.

- `width-probe.stdout`: 7,996 bytes,
  `3c52825555860f2040aa4510dbd5553bb01cdb722439d4c4d2e2d9dce7674e86`;
- `width-probe.stderr`: 0 bytes,
  `e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855`;
- framed empty `ops.bin`: 25 bytes, zero uncompressed operations,
  `9d41f487bf780fe99a619854b3b59245fb7dec4519e19708bddfdd08dbd2a48f`;
- `width-schedule.tsv`: 7,809 bytes,
  `07e910b9fec965a15b7d3f97ef321320c5202827fca9e6761c69f1979de6f235`;
- `active-widths.tsv`: 21,578 bytes,
  `c49b95b00f2c0c33936b1123f847cf7d2d17f17eda3c89a18d3d8e322aea8bf5`;
- `native-inputs.txt`: 856 bytes,
  `c226f964ae42d80d94eb9850fe580c8ffc27951a5a64a581a5d69d476f2003a8`.

The phase metadata, generated phase schedule, and checkpoint were separately
regenerated byte-for-byte before this gate at SHA-256 `fba2270e...bca0b`,
`17720a68...43d35`, and `75deeae0...b937f`; the inherited 4,096-byte XOF
self-check passed.

## Verdict

`q1270_native_recurrence_v1` clears the source-identity, round, width,
explicit target-config, phase-geometry, and checkpoint-input gate. The next gate is
an empty-file implementation of the target-native recurrence, host-state
digest, and target phase composition, followed by two independent builds and
complete inherited-mask equality. The unopened D32 and future F16 remain
untouched.
