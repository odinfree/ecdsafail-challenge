# Burn Q1270 routed-control checkpoint state

## Protected leader

Exact official ancestor `4eb93cb`; composed source `a220903`; protected local
Q1271 component receipt and hashes are frozen in `PREDECLARATION.md`.

## Overturn ledger

| wall | load-bearing assumption | overturn observation | cheapest falsifier |
|---|---|---|---|
| multiply fold holds Q1271 | every derived selector must stay live until cleanup | a derived selector can be recomputed from parents after the fold | standalone lifecycle miter |
| Q1270 requires a more approximate adder layout | all four co-binders must spend another ladder notch | one exact lifetime cut removes a fold owner while geometry lowers the other owners | three-row profile ablation |
| existing Q1271 families exhausted local aliases | target0/sign and doubled-out were the last redundant wires | `sign_xor_add` is dead after `routed` construction until cleanup | source use-def audit plus peak trace |

## Explorer board

| lineage | ancestor | target | correctness | saddle budget | state |
|---|---|---|---|---|---|
| routed-control checkpoint | `a220903` | Q1270, else exact T cut | pending | one miter, one implementation, three profile rows | PREDECLARED |

## Ancestor ladder

1. `4eb93cb`: official Q1273 source.
2. `7342270`: selector lifecycle port.
3. `b32c2d0`: selector plus doubled-out prototype.
4. `a220903`: exact target0/sign alias, Q1271 component-clean source.

## Composition matrix

| lever | selector evict | doubled-out evict | target0/sign alias | routed checkpoint |
|---|---:|---:|---:|---:|
| selector evict | inherited | PASS | PASS | pending |
| doubled-out evict | PASS | inherited | PASS | pending |
| target0/sign alias | PASS | PASS | inherited | pending |
| routed checkpoint | pending | pending | pending | predeclared |

Next action: commit/push this predeclaration, then reproduce the protected
profile before any candidate source edit.
