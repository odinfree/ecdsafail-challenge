# E-012 phase-clean call-9 carry erasure

Frozen on 2026-08-23 before E-012 source edits, temporary instrumentation,
candidate operation output, or measurement. This isolated lane starts from
pushed E-011 terminal commit
`d60b9c892fcbd0a67025be62fa00e2146ba6c1ea` on branch
`research/q1272-call9-phase-clean`.

## Fixed identity and allowance

- inherited E-007 switches:
  `SUB4_PP_REPLAY_FOLD_WINDOW=55 SUB4_PP_MUL_PLUS2F_ALIAS=1
  TLM_FINAL_Y_SUB_BOUNDARY_CARRY=1`;
- inherited operations/SHA: 12,943,345 /
  `4618d4af86c23c06cc72fef26f8a8e5986c7a9de5f804891541a8ce6eb189f37`;
- exact inherited H64 T: 918113.406063 at Q1272;
- live Q1272 rounded-T ceiling: 919105;
- E-011 focused square price: `+606.297` executed T at Q1272;
- remaining conservative diagnostic allowance after E-011: about 386 T;
- protected arithmetic, product-square, evaluator, and tracked-results hashes:
  `c6deb3c06ef565071ff076f0cdfbca6aa483e8be555a24ea0ff94e52e87e69e0`,
  `21e4ef934d3e55b2bcff247dfcb509c67ca20ab1ed39614aa79299c8fc51a4c6`,
  `b35314bc47a5f8eecbf60459e6c76c9b43aa034eeb9b7ec3421655d74f9e890b`,
  and `eea840222afd2424445550252f25795d63eb4bc3904b8c87f0213b5f22abc810`.

E-011 already proves the call-9 apex-carry mechanism exact on all eight frozen
values and clean in the ancilla channel. Its sole failure is phase mask `0x10`
on lane 4, `z = 2^57-f`. E-012 may repair only that phase erasure. It may not
move the width boundary, change the carry predicate, change the high200
increment, or weaken any E-011 value case.

## Predeclared localization

Recreate E-011C1 byte-for-byte as the held candidate and tag, without changing
semantics, every measurement cleanup in the call-9 clean `+f` ladder and the
one high200 Khattar-Gidney controlled increment. Run the unchanged eight-lane
miter with its fixed SHAKE seed and step the operation stream. For each tagged
free/HMR/CZ group, record the pre-HMR target Boolean, its correction controls,
the measurement bit, and the phase delta on lane 4.

The E-011 durable packet preserved the eight values and their order but omitted
the simulator seed literal; searches of its worktree, temporary directories,
central state, and commits found no surviving harness. Before the first E-012
run, this provenance gap is repaired by freezing the natural label
`b"e011-call9-boundary-miter"`. E-012 therefore reproduces the exact frozen
input ensemble and order, but does not claim byte-identical seed continuity
with the removed E-011 harness.

The primary hypothesis is the increment's `vent_xc` path. That path toggles a
prefix ancilla before measurement, so the measured Boolean is
`NOT (x AND y)`. HMR contributes `m * NOT(x AND y)` while the existing
classically conditioned CZ cancels only `m * (x AND y)`, leaving `m`. The
missing feed-forward is a classically conditioned global negation by `m`.

Localization passes only if the surviving lane-4 kick is attributed to an
exact tagged complemented-AND cleanup and all prior/following cleanup groups
compose as recorded. If the phase appears in the clean ladder, cannot be
attributed uniquely, or value/ancilla differs from E-011, stop without a
repair. Temporary trace contexts and harnesses are never durable source.

## Sole admitted repair

Only after the localization gate passes, E-012C1 may make complemented-AND
vent cleanup phase-exact inside the single call-9 controlled increment:

```text
HMR(t -> m); CZ(x,y) if m; NEG if m
```

The final `NEG if m` cancels the residual `m` for
`t = NOT(x AND y)`. It is classical feed-forward and adds zero Toffoli. The
ordinary `t = x AND y` vent path remains unchanged. The correction must be
scoped behind `TLM_SQUARE_C_HALF_BOUNDARY_PHASE_CLEAN=1`; protected defaults
and every non-call-9 increment remain byte-identical. No site-specific value
toggle, alternative predicate, width change, or second repair is admitted.

## Frozen gates

1. Re-run the unchanged eight-lane adversarial miter. Require all eight full
   256-bit values exact, phase mask `0`, and every non-register qubit zero.
2. Run the unchanged 64-lane `product-register-square-inputs` corpus. Require
   exact value, phase, and ancilla for every lane.
3. Build the full E-007 plus E-012 composition and reproduce Q1272. Run the
   frozen same-seed 64-lane diagnostic; require rounded T at most 919105 and
   no classical, phase, or ancilla regression on that diagnostic.

Stop at the first gate failure. No full 9,024-shot evaluation, H64, scan,
hunt, remote/provider action, spend, submission, adaptive repair, or generated
artifact commit is authorized in E-012. Freeze and push either terminal result.

## Terminal localization

The repaired-provenance seed produced the same logical E-011 failure class:
all eight full values were exact, every non-register qubit was zero, Q515, and
817 emitted Toffolis. Its phase mask was `0x30`, not the removed harness's
recorded `0x10`; this is consistent with the explicitly unavailable simulator
seed continuity and is not presented as a numeric reproduction.

The operation-step trace falsified E-012's sole admitted repair. Every tagged
Khattar-Gidney vent cleanup had its HMR target exactly equal to the CZ control
product on all eight lanes and contributed phase delta zero. Every tagged
clean `+f` ladder cleanup did the same. Cumulative phase remained zero through
the last low-ladder HMR/CZ group. It changed to `0x30` only in the remaining
downstream `clear_overflow_phase` shell.

Therefore no complemented-AND measurement exists to correct with the admitted
conditioned `NEG`. E-012C1 was not implemented. The frozen first gate failed,
so square64, composition, and the Q/T diagnostic were not run. All temporary
trace contexts, candidate plumbing, and the miter harness were removed.
Protected source/results hashes reproduce exactly.

Terminal verdict: `HYPOTHESIS_FALSE; DOWNSTREAM_OVERFLOW_PHASE_LOCALIZED;
SOURCE_REMOVED`. The next structural action requires a new predeclaration: run
the inherited overflow-phase erasure while the low56 clean ladder and apex
carry are still live, before the high200 correction changes its comparison
state. That is an ordering experiment, not a second arithmetic repair.
