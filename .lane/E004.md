# E-004 fixed-shot square/result isolation

Predeclared on 2026-08-23 before any new fixed-shot instrumentation or repair
measurement. This continues clean commit
`d34406376f3cfa41279d3dde93a4ef0d51a5ac00`.

## Frozen stream and corpus

- protected source defaults remain fold54 / selector alias off;
- diagnostic stream is reconstructed only with
  `SUB4_PP_REPLAY_FOLD_WINDOW=55 SUB4_PP_MUL_PLUS2F_ALIAS=1`;
- unpatched artifact guard: 12,939,336 operations, SHA-256
  `d4ecab6dd80d044d0a28ae456a9106992d2b3aae070d80cfa0dde3d24400f920`,
  Q1272;
- failing fixed shot: nonce `444000000032`, shot 7997, exact result mask 16;
- adjacent clean instrumentation control: the same nonce, shot 7996;
- H parity block remains exactly `444000000000..444000000063`, all 64
  complete 9024-shot evaluations if a source repair is admitted.

No new nonce, adaptive shot, or search range may be added.

## Independent intermediate contract

For input target `(tx,ty)`, offset `(ox,oy)`, and
`lambda=(ty-oy)/(tx-ox) mod p`, snapshot the public x/y registers after the
existing profile phase boundaries and compare against:

```text
after tlm_coord_add3x:
  x = tx - ox + 3*ox
  y = lambda
after square_product_register:
  x = tx - ox + 3*ox - lambda^2 = ox - rx
  y = lambda
after pp_mul_restore:
  x = ox - rx
  y = lambda * (ox - rx)
after tlm_coord_y_sub_final:
  x = ox - rx
  y = lambda * (ox - rx) - oy = ry
after tlm_coord_rsub_final:
  x = ox - (ox - rx) = rx
  y = ry
```

All arithmetic is modulo secp256k1 p. The tool must verify the base artifact
SHA and op count before applying the frozen tail nonce in memory, reproduce the
same Fiat-Shamir derivation and batch/lane as the unchanged evaluator, and run
all preceding batches so measurement RNG state is not silently changed.

## Attribution hypotheses

1. **E-004S square:** the first value divergence is immediately after
   `square_product_register`. Only then may a one-site square repair be built.
2. **E-004M multiply/replay:** square is exact and the first divergence is at
   `pp_mul_restore`. Stop; do not widen square or final-shell windows.
3. **E-004Y final y-sub:** multiply output is exact and the first divergence is
   at `tlm_coord_y_sub_final`. Only then may its one responsible reduction
   window be repaired.
4. **E-004X final rsub:** y-sub is exact and the first divergence is at
   `tlm_coord_rsub_final`. Only then may its one responsible reduction window
   be repaired.

If more than one boundary is already wrong, the clean control disagrees, the
phase boundary cannot be reproduced exactly, or attribution is otherwise
ambiguous, stop at evidence. No source repair is authorized by an ambiguous
trace.

## Repair gate

A repair, if admitted, must change only the attributed primitive and retain an
explicit opt-out. First require the failing fixed shot to become classically
exact while shot 7996 stays exact. Then require its focused primitive miter,
Q/T profile, complete inherited 9024-shot `cls/pha/anc`, and unchanged H64
full-simulator block. It must remain Q1272, rounded full T at or below 921139,
ancilla zero, and introduce no result fault on H. A fixed-shot repair that
merely moves the fault to another channel or H row is killed immediately.

Protected default and promoted-route hashes must reproduce before and after any
opt-in experiment. No hunt, provider, spend, or submission action is allowed.

## E-004Y-R1 admitted repair (pre-measurement)

The frozen trace localized the first and only value divergence to the final
`tlm_coord_y_sub_final` boundary. Shot 7997 is exact through multiply restore,
then its y coordinate is exactly `2^53` above the independent field result;
shot 7996 stays exact. This equals the first bit outside the current 53-bit
`+f` reduction window (`LSBS = 53`), rather than a square, multiply, or rsub
error.

Before building or measuring a repaired artifact, E-004Y-R1 declares the one
allowed repair: extend only the final y-subtract `+f` reduction window from 53
to 54 bits behind `TLM_FINAL_Y_SUB_LSBS54=1`. Every other subtraction and the
protected default retain 53. The predicted effect is to propagate the observed
carry into bit 53 and remove exactly the `+2^53` output delta. No second window,
alternate width, or adaptive repair will be tried if this falsifier fails.

The first gate is still only shot 7997 plus adjacent control 7996 on their
unchanged Fiat-Shamir batch. Failure to make 7997 exact, any control regression,
nonzero phase/ancilla, Q above 1272, or rounded T above 921139 kills R1 before
H64. Passing that gate admits the focused primitive miter, inherited full
evaluation, and the already frozen H64 block; it does not authorize any new
nonce or scan.
