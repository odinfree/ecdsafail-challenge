# E-009 fixed-shot result localization

Predeclared on 2026-08-23 before any E-009 phase trace, fixed-shot simulator
output, or repair. This starts from clean pushed commit
`868a1b5724e5dc631af82a86bde34c4c32637e54` and retains E-007 unchanged:

```text
SUB4_PP_REPLAY_FOLD_WINDOW=55
SUB4_PP_MUL_PLUS2F_ALIAS=1
TLM_FINAL_Y_SUB_BOUNDARY_CARRY=1
```

The campaign root refreshed live SOTA to source `b523ecf`, score
`1,169,101,620`. At Q1272 the strict rounded-T ceiling is now 919105. E-007's
exact H64 T918113.406063 retains 992 rounded-T headroom but remains dirty.

## Frozen stream and inputs

- candidate inherited artifact: 12,943,345 operations, SHA-256
  `4618d4af86c23c06cc72fef26f8a8e5986c7a9de5f804891541a8ce6eb189f37`;
- tail nonce: `444000000042`;
- exact tail-bound SHA-256:
  `89d26f325df66ba0ebd9ec327895ccb537a918f0c7b0d34d9c8002a094f9d968`;
- failing input: shot 3036, exact classifier mask 16 (`result`);
- adjacent control: shot 3035, absent from the complete mismatch set;
- unchanged E-008 row: classical/phase/ancilla `14/11/0` for the full 9024
  shots of this tail-bound stream.

No other nonce, shot, repair, or corpus may be introduced during localization.
The probe must derive the original target/offset pairs from the exact
tail-bound operation stream and replay every prior batch so measurement RNG
state is unchanged.

## Frozen checkpoints and hypotheses

Snapshot public x/y at balanced phase boundaries after:

1. `tlm_coord_add3x`;
2. `tlm_square`;
3. `tlm_forward_multiply`;
4. `tlm_coord_y_sub_final`;
5. `tlm_coord_rsub_final` and final tail.

Compare both frozen inputs against the independent secp256k1 relations already
used by E-004. Attribute the first divergence to exactly one of square,
multiply/replay, final y-sub, final rsub, or a later tail. If the control
diverges, more than one boundary is already wrong, the condition stack is not
balanced at a cut, or the trace does not reproduce final values and
phase/ancilla exactly, stop without a repair.

## Admission boundary

Localization is the only currently admitted source-free experiment. A
decisive single-boundary result may name one smallest causal repair, but that
repair must be separately predeclared and statically priced before source
changes. Any repair must keep an opt-out, Q1272, rounded T at or below 919105,
exact protected hashes, focused reversible miters, unchanged inherited full
evaluation, and the fixed H64 gate. Failure at the first repair gate stops the
turn; no adaptive alternative follows.

No hunt, scan, provider/remote action, spend, submission, or generated artifact
commit is authorized.

## Terminal localization

The exact tail-bound stream reproduced 12,943,345 operations and SHA-256
`89d26f325df66ba0ebd9ec327895ccb537a918f0c7b0d34d9c8002a094f9d968`.
All six cuts were statically balanced at condition depth zero. The unchanged
Fiat-Shamir derivation replayed batches 0 through 47 before splitting batch 47
at these production operation boundaries:

| checkpoint | end operation |
|---|---:|
| after add3x | 6,014,550 |
| after product-register square | 6,935,815 |
| after multiply restore | 12,920,661 |
| after final y-sub | 12,931,278 |
| after final rsub | 12,943,249 |
| after tail | 12,943,345 |

Control shot 3035 was exact in x and y at every checkpoint. Its phase bit and
non-public dirty-qubit count were both zero at every cut. Target shot 3036 was
also exact through add3x. Its first divergence was immediately after the
product-register square:

```text
expected x = 96f9be2f5b2505cdac4808634bd20b3a942774d73903f78e1117d196633bc29e
observed x = 96f9be2f5b2505cdac4808634bd20b3a942774d73903f78e1217d196633bc29e
delta      = +2^56
```

The target y/lambda was still exact at that cut; phase and non-public dirty
count remained `0/0`. Multiply then propagated the wrong x into y, the final
y-sub preserved that causal error, and rsub changed the x delta to `-2^56`.
The tail changed neither coordinate. This accepts the square hypothesis and
rejects multiply, final-y, rsub, and tail as first causes for this event.

A separate integer replay of the nine `mod_add_top` reductions matched the
observed square output exactly. Only call 9 of 9 crossed its truncated
low-window boundary: the final half-shift subtraction of the low 128 bits of
`C=(lambda_low+lambda_high)^2`. Its overflow was one, and adding
`f=2^32+977` to the low 56-bit post-add value carried across bit 56. None of
the preceding eight top reductions crossed its boundary. At that ninth site,
bit 56 was zero, so a one-site 56-to-57 window is the smallest pinned-input
falsifier; it is not yet an exact general repair because a carry can recur at
bit 57.

Temporary boundary printing and trusted-evaluator instrumentation were
removed. The evaluator source, tracked results, and product-square source
remain at SHA-256 `b35314bc47a5f8eecbf60459e6c76c9b43aa034eeb9b7ec3421655d74f9e890b`,
`eea840222afd2424445550252f25795d63eb4bc3904b8c87f0213b5f22abc810`,
and the committed E-007 source respectively. No generated row was appended.
