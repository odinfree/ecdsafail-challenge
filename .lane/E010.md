# E-010 one-site square-window falsifier

Predeclared on 2026-08-23 before any E-010 source change, candidate artifact,
fixed-input output, or focused miter measurement. This starts from clean pushed
E-009 terminal commit `a2709f7` and retains the E-007 route unchanged:

```text
SUB4_PP_REPLAY_FOLD_WINDOW=55
SUB4_PP_MUL_PLUS2F_ALIAS=1
TLM_FINAL_Y_SUB_BOUNDARY_CARRY=1
```

The live Q1272 rounded-T ceiling is 919105. E-007 H64 T918113.406063 leaves
992 rounded-T headroom.

## Causal cell and static price

E-009 proved that target shot 3036 first diverges after the product-register
square by `+2^56`. Integer replay isolates the only crossed fold boundary to
`mod_add_top` call 9/9: the final half-shift subtraction of the low 128 bits of
`C=(lambda_low+lambda_high)^2`. Its overflow is one and its post-add low56 plus
`f=2^32+977` carries across bit 56. The first eight top reductions do not cross
their boundary.

Source-free allocator instrumentation measured call 9 at Q901 on entry,
through its fold, and on exit; global peak was already Q1272 elsewhere. One
extra clean carry wire therefore has 371 qubits of local slack and cannot raise
the global peak. Extending a clean constant-add window by one bit is bounded to
one extra carry cell and single-digit emitted/realized T, far below the 992-T
ceiling. The instrumentation changed no operations and was removed before this
predeclaration; product-square source SHA-256 is
`21e4ef934d3e55b2bcff247dfcb509c67ca20ab1ed39614aa79299c8fc51a4c6`.

## One admitted implementation

Add only `TLM_SQUARE_C_HALF_LSBS57=1`. When set, the final C-half
`mod_add_top` uses a 57-bit `+f` window; all other eight square top reductions,
all replay cells, all coordinate cells, and the protected default remain at
their inherited widths. The environment variable is the explicit opt-in;
absence must reproduce all protected hashes.

No second width, different call, boundary predicate, or adaptive repair may be
tried if this implementation fails.

## Frozen gates

1. Pin the original E-009 square inputs, independent of the candidate SHA:
   control 3035 and target 3036 use their exact original lambda/add3x values.
   Require both square outputs exact, phase zero, and every non-public qubit
   clean. Target 3036 is predicted to lose its observed `+2^56` delta because
   bit 56 at the crossed site is zero.
2. Run one predeclared local `mod_add_top` miter with overflow-on values on both
   sides of `2^56-f` and `2^57-f`, plus overflow-off controls. Compare the
   complete 256-bit result against exact modular shifted subtraction and
   require phase/ancilla zero. This is the generality gate: a 57-bit window is
   killed if it merely moves the lost carry to bit 57.
3. Only if both gates pass, build the full E-007 composition, require Q1272,
   rounded full T at most 919105, product-square and affine selftests, inherited
   unchanged 9024-shot evaluation, and the frozen E-008 H64 causal gate.

Stop on the first failure. No hunt, scan, provider/remote action, spend,
submission, or generated artifact commit is authorized.
