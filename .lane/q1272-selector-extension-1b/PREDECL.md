# Superseded Q1272 selector one-billion extension predeclaration

Terminal status: `SUPERSEDED_UNACTIVATED`. Live-source commit `7342270`
replaced this older `14608572` stream before any range declaration, host
assignment, provider action, or compute. Keep this packet only as an auditable
record of the completed five-million pilot and rejected extension geometry.

This packet planned one bounded extension of the already-qualified Q1272 selector
stream. It does not declare the ranges or authorize provider use. The only
admissible stream is source `14608572e84daf89397768c43ac0d812c714c3bd`,
12,908,488 operations with SHA-256
`678c149f65ce00cd44caada6a534817e8b69f993913c509a8b396b6f526c22f0`,
and checkpoint SHA-256
`0dbd37238642df839b7672e25a85d2d5183c202102f80388de749feaecf1c361`.

The frozen board snapshot is source `4eb93cb`, Q1273/T914243, score
`1,163,831,339`. At Q1272 the strict rounded-Toffoli ceiling is `914,961`.
The already-qualified stream's measured T914727.660 rounds to 914,728 and
scores `1,163,534,016`, a frozen-snapshot margin of `297,323`.

The never-declared proposed interval was the immediate half-open successor to the completed
five-million pilot:

`[100000036457280,100001036457280)`

It contains exactly 1,000,000,000 nonces. `SHARDS.tsv` divides it into 20
contiguous 50,000,000-nonce slots. Each slot is processed in 1,000,000-nonce
chunks using the exact complete 9,024-shot classical predictor. The range
checkpoint cannot advance until every predicted classical-zero survivor has a
terminal unchanged full 9,024-shot evaluator receipt. Only `0/0/0` with rounded
T at most 914,961 can become a candidate.

Hard stops:

- no provider start, host assignment, or range declaration is included;
- no early-exit or approximate screen is admitted;
- no survivor cap, skipped confirmation, or partial evaluator result is valid;
- no source, operation, checkpoint, binary, or receipt drift is tolerated;
- no automatic restart, reassignment, extension, submission, or public note;
- the live frontier must be reopened and the score ceiling recomputed before
  any separate activation authorization is created.
