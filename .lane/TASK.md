# Q1274 paired-cap control predictor qualification

Predeclared before source/model edits. Work only in this isolated worktree and
branch. Read the burn-the-house-down skill, the Fable structural state, and the
Q1276 missing-channel audit before implementation.

## Exact target

Starting source is live `b523ecf`. The env-driven two-co-binder stream

```text
SUB4_PP_PEAK=1274
SUB4_SQUARE_LADDER=244
```

has frozen identity:

- 12,935,433 operations;
- SHA-256 `61a57ce6e167b64663288ade584785b26562f61ff197f6ae0c11c85ea098ee8e`;
- Q1274, average T917227.881, rounded score 1,168,548,472;
- strict margin 553,148 below the frozen live score 1,169,101,620;
- inherited nonce 81327465284 full 9,024-shot result `14/9/0`;
- focused product-square and complete point-add selftests pass exactly with
  square/replay co-binding at Q1274.

Authoritative structural evidence is
`/Users/olifreuler/Documents/Codex/2026-08-21/par/work/fable-b523-ladder-below1276/.lane/STATE.md`.
Reopen live before any final score claim.

The sibling Q1276 audit localized a general lost low53-to-high203
coordinate-shell carry and froze a nonce-independent model correction on
`research/b523-q1276-model-qualify@7204c6d`. Import it only after that lane's
original H64 and blinded-disjoint gates close; revalidate it here rather than
assuming geometry independence.

## Gates

1. Reproduce the env target exactly, then hard-code only PEAK1274/LADDER244
   behind an opt-out that reproduces protected b523 byte-for-byte. The baked
   default must be byte-identical to SHA `61a57ce6...`; source diff must contain
   no other circuit change.
2. Before revealing outcomes or modifying model semantics, freeze paired H64
   nonces for protected b523 and Q1274 plus a deterministic disjoint32 corpus.
   Run unchanged full 9,024-shot evaluation on the paired H64 and establish
   non-inferior classical/phase density and ancilla zero before predictor work.
3. Port exact current-source geometry into the CPU model. Require complete
   9,024-shot classical fault-set equality, not just count equality, on H64.
   If needed, import the general low53/high203 correction and commit it before
   revealing disjoint32. Require 32/32 exact fault-set equality there.
4. Build a source-bound phase screen from the proven Gidney-divergence model if
   it can be cheaper than full evaluation. It must reproduce exact phase masks
   on a predeclared corpus; otherwise label it diagnostic-only and do not use
   it as a fleet filter.
5. Port the exact classical screen to CUDA. Require CPU=CUDA full 9,024-bit
   masks, popcount equality, source/operation/checkpoint guards, and wrong-
   stream fail-closed negatives on a frozen 32-fixture packet.

Any predictor-only classical fault blocks scan use. Underprediction is
canary-only. No nonce range, scan, hunt, provider creation/retarget,
submission, public note, or incumbent edit is authorized until all preceding
gates close. Final validity remains unchanged full 9,024-shot `0/0/0`, and the
small 553,148 score margin requires a fresh measured T plus fresh live refresh.
Do not commit ops.bin, result rows, binaries, logs, temporary evaluators,
caches, or generated artifacts. Update durable state, commit, and push at each
terminal gate.
