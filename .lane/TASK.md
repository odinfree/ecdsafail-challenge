# Q1274 b523/B1 predictor qualification

This lane is predeclared before changing source or model semantics. Work only
in this isolated worktree and branch. Read the campaign burn-the-house-down
skill and the sibling evidence named below before implementation.

## Frozen target

Live source anchor: `b523ecf`, score 1,169,101,620. The structural Fable lane
measured the exact composition

```text
SUB4_PP_BREAK_1=24
SUB4_PP_PEAK=1274
SUB4_SQUARE_LADDER=244
```

at Q1274, full-evaluator average T913684.649, 12,879,923 loaded operations and
projected rounded score 1,164,034,690 (5,066,930 below the frozen live score).
Its inherited nonce is classically dirty. The final operation SHA and baked
source commit must be imported from
`/Users/olifreuler/Documents/Codex/2026-08-21/par/work/fable-b523-ladder-below1276`
after that lane freezes them. No operation identity may be inferred from these
numbers alone.

This branch begins at the already hard-coded B1=24 source/model commit
`6f6da43`, whose B1-only stream passed exact CPU/CUDA count and full 9,024-bit
mask parity on 32 predeclared fixtures. Evidence:

- `/Users/olifreuler/Documents/Codex/2026-08-21/par/work/b523-b1-24-predictor/.lane`
- `/Users/olifreuler/Documents/Codex/2026-08-21/par/work/b523-b1-24-gpu-parity/.lane`

The sibling Q1276 audit localized its only model miss to a general lost
low53-to-high203 coordinate-shell carry and froze a nonce-independent value
correction at `research/b523-q1276-model-qualify@7204c6d`. Import that correction
only after its original H64 and blinded-disjoint gates finish; geometry
independence is a hypothesis to revalidate here.

## Gates

1. Hard-code only PEAK=1274 and SQUARE_LADDER=244 on this B1=24 base. Require a
   clean build to be byte-identical to the Fable target SHA/operation count and
   reproduce Q1274/T913684.649 within exact evaluator precision.
2. Freeze a 64-nonce H64 corpus and a deterministic disjoint32 corpus before
   inspecting trusted outcomes or changing model semantics.
3. Port exact current-source geometry into the CPU model. Require complete
   9,024-shot classical fault-set equality, not only counts, on H64.
4. Only if required by a source-semantic mismatch, import the frozen general
   coordinate-shell correction. Commit the correction before revealing
   disjoint32. Require 32/32 exact fault-set equality there.
5. Port to CUDA and require CPU=CUDA full 9,024-bit masks, popcount equality,
   source/operation/checkpoint guards, and wrong-stream fail-closed negatives
   on a separately frozen 32-fixture packet.

Any predictor-only fault blocks scan use. Predictor undercount is canary-only,
never fleet-safe. No nonce range, scan, hunt, provider creation/retarget,
submission, public note, or incumbent edit is authorized in this lane. Final
candidate validity remains unchanged full 9,024-shot `0/0/0`. Do not commit
ops.bin, results, binaries, logs, temporary evaluators, caches, or generated
artifacts. Update durable state, commit, and push after each terminal gate.
