# Live-source Q1272 combined classical/phase predeclaration

Status: `PREDECLARED / NO MODEL OUTPUT / D32 ORACLE SEALED`.

This lane qualifies a CPU classical plus conditional-phase screen for the
exact Q1272 selector-eviction stream.  It does not authorize CUDA, a provider,
a nonce range, a hunt, a fleet action, a submission, or a public note.

## Live and source identity

The live benchmark and all submissions were reopened immediately before this
predeclaration.  The promoted frontier remains commit
`4eb93cb33bbf6a93229fe166b8d511c5e52ee253`, Q1273 / rounded T914243 /
score `1,163,831,339`.

- lane branch: `research/q1272-live-phase-screen`;
- evidence base commit / tree:
  `41dd0b4508527081b8d24255adf0579389f02453` /
  `04fee126fd8fb3112e7422bddb2e458dd38adbc6`;
- exact structural source commit / tree:
  `73422709ed70ba9725b3cb592770bcf197df4cdb` /
  `fe77bddfb49b426312b1cca3d009b150cd06fd89`;
- `pingpong_div.rs` / `mod.rs` SHA-256:
  `6e7cce578663d8419c032190a9caa405ca0d84e0512104b766b9c68540f01994` /
  `0c9e9a920d9078f2390028009895f03d495096fdac9d2b596c7913c064e03e63`;
- expected operation count / SHA-256:
  `12,904,643` /
  `ea19759d80a4bc1492a5e98c966e70ee79d5aa40211e6cc480b08cf032c8a0f1`;
- exact build environment: `SUB4_PP_FOLD_SELECTOR_EVICT=1`,
  `SUB4_PP_PEAK=1272`, `SUB4_SQUARE_LADDER=242`, every other `SUB4_*`
  variable unset;
- inherited nonce / unchanged full result:
  `65700024945645`, Q1272 / T914783.521 / `23/8/0`, first classical
  mismatch shot 292.

No model or oracle result is admissible until a forced build reproduces the
expected operation count and SHA byte-for-byte.

## Frozen donors and portability boundary

Only the already-qualified Q1273 combined CPU machinery may transfer:

- terminal combined source commit / tree:
  `3c1a49cdde48f804c4e35bd0501d7d8d7f46f15e` /
  `70ab3c0196b85abd74835399094642aeb828b323`;
- fixed-density evidence commit / tree:
  `bb6058366070e277c4e9a7c2000fe114f42f8438` /
  `69f211c738af6a5ca6d9f6b7752d98b9b777cbc0`;
- donor combined model / CPU SHA-256:
  `14fc9230b55d2b2a84721e50d860933764a18d1357ad30331e5999280f764261` /
  `1d93d46803670324401443ed9f625ee641c6fa8ef77ba4a920baebff83ea24ce`;
- donor phase-schedule generator / deterministic composer SHA-256:
  `83bfa142f2cf546ea5673de424597bf26ed3a57db0173d2b177a009ad9f8e966` /
  `2c0710e0d279bbfbd1424f2cd48f57b661686352238697a7552e0c6ad756d01a`.

Portable invariants are limited to the finite-width walk/replay recurrence,
source-event checkpoint method, phase-event family semantics, canonical mask
framing, and final predicate:

```text
clean_phase_mask = raw_phase_mask & ~classical_mask
survivor = classical_mask == 0 && clean_phase_mask == 0
```

The Q1273 operation count, SHA, state digest, phase ordinals, schedule/table,
checkpoint, constants, fixtures, and binary are not portable.  They must be
regenerated and requalified from the exact Q1272 source/stream.  The donor's
current 696-arm classical canary predicts 17 inherited faults while trusted
Q1272 truth is 23, with the first trusted failure at shot 292; therefore it is
a partial semantic donor, never a qualified Q1272 classical screen.

## One allowed semantic family

Before any D32 oracle result is opened:

1. mechanically import the donor host/model/CLI and schedule-generation
   machinery into a Q1272-specific namespace;
2. regenerate operation-site geometry and the R/Hmr phase schedule from the
   exact Q1272 stream, binding count, SHA, family order, ordinal count, and a
   source-state checkpoint;
3. re-derive every Q1272 round/window/default constant from source rather than
   copying Q1273 stream constants;
4. preserve the finite-width wrapped-register and replay semantics; localize
   any inherited/H64 mismatch to a source mechanism and correct that mechanism
   generically, without a nonce/shot special case or ideal-output fallback;
5. compose conditional phase only after complete classical masks are exact.

No oracle-derived lookup, witness exception, approximate pred0, tolerant
retention, schedule reorder, raw-phase claim on classically dirty shots, or
alternate survivor predicate is allowed.

## Frozen corpora and reveal order

- inherited singleton: nonce `65700024945645`;
- H64 calibration corpus: decimal LF list `444000000000..444000000063`,
  SHA-256
  `17443162349d50e521b0c9a20904de5a0212a668e6f3ae7ea0e2702b2f79c4b9`;
- disjoint D32 holdout: the exact ordered nonce list committed at Q1273
  combined commit `3c1a49c`, SHA-256
  `62bca2420f684c718e52b516dc850f05d72c33f64798002fe2afd215642a0f90`.

All Q1273 mask results are source-incompatible and must not be reused.  A
temporary, hash-bound oracle may add output-only instrumentation to the
unchanged trusted evaluator semantics, but must preserve input derivation,
operation application, reference arithmetic, classical comparison, phase,
and ancilla checks.  Its inherited aggregate must reproduce `23/8/0`; source,
instrumentation patch, binary, corpus rows, and manifest hashes are required.

Reveal order is fixed:

1. reproduce Q1272 ops byte-exact and seal oracle/model source hashes;
2. inherited complete classical and conditional-phase masks;
3. H64 complete masks and any source-semantic repair;
4. commit the terminal model family and expected guard identities;
5. only then reveal D32 oracle masks and compare without edits;
6. deterministic repeat after D32.

## Terminal gates

CPU `GO` requires all of the following:

1. exact source, operation count/SHA, state checkpoint, generated phase
   schedule, corpus, and deterministic SHAKE256 identities;
2. complete per-shot classical-mask equality on inherited + H64 + D32;
3. complete per-shot conditional-phase-mask equality on inherited + H64 +
   D32, using `raw_phase & ~classical` only;
4. ancilla zero on every oracle fixture;
5. byte-identical deterministic repeats of inherited and the final D32 row;
6. wrong magic, wrong count, same-count/wrong-SHA, wrong state/checkpoint,
   wrong schedule, missing stream, malformed/non-canonical/overflowing nonce,
   malformed/out-of-range shot, unknown selector, and disabled scan mode all
   reject with empty result output;
7. canonical sorted shot indices and unchanged ordinary output framing.

Any mask mismatch, guard ambiguity, unclassified classical channel, phase
event-count drift, or nonzero ancilla is terminal `HOLD`.

After exact qualification, characterize only the already frozen H64 corpus.
Report classical fault lambda, conditional-phase lambda among classically clean
shots, joint lambda, empirical zero counts, a conservative upper mean, Poisson
zero-density planning estimate, and 95% sizing.  This is a fixed diagnostic,
not a range or hunt and cannot authorize one.

All binaries, `ops.bin`, oracle masks, raw logs, and generated tables remain
outside Git.  Only source, scripts, compact hash receipts, and manifests may be
committed.
