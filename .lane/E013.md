# E-013 reorder overflow erasure before high carry propagation

Frozen on 2026-08-23 before E-013 source edits, temporary harnesses, candidate
operation output, or measurement. Base is clean pushed E-012 terminal commit
`c441629` on `research/q1272-call9-phase-clean`.

## Causal premise

E-011 proves the low56 apex carry and high200 increment value-exact at Q1272
for `+606.297` focused-square T. E-012 proves every new clean-ladder and CINC
HMR/CZ group phase-exact. The phase appears only afterward because
`clear_overflow_phase` recomputes its erasure predicate from high output bits
after E-011 has already propagated the low carry into those bits. The inherited
erasure was derived for the unpropagated high state.

E-013 changes only ordering and carry representation. It must not change the
low56 width, apex predicate, high200 direction, overflow comparator, or any
frozen value.

## Sole admitted composition

At the clean `+f` ladder apex, compute the same E-011 Boolean
`b = carry55 AND NOT sum55`. Before applying the high200 increment, invoke the
existing `clear_overflow_phase` on the unchanged high output and addend. Retain
its measurement bit `m_o`. The HMR leaves the overflow qubit at zero, so restore
that qubit with classically conditioned `X(m_o)` only long enough to preserve
the existing clean-ladder reverse pass.

Then execute the unchanged high200 controlled increment using coherent `b`,
recompute/uncompute `b` from the still-live ladder apex, and run the inherited
ladder cleanup. Finally apply the same classically conditioned `X(m_o)` to
return the restored overflow qubit to zero. The outer call must skip its old,
now-consumed overflow erasure.

This is one measurement-adaptive reorder: the original erasure runs before
the state it authenticates is mutated, while its classical result supplies the
same control value needed by the remaining reverse pass. It adds two
classically controlled X operations and no Toffoli, comparator, predicate, or
arithmetic operation. No second repair is admitted.

## Static price and identity

- E-007 inherited identity: 12,943,345 operations /
  `4618d4af86c23c06cc72fef26f8a8e5986c7a9de5f804891541a8ce6eb189f37`;
- inherited exact H64 T/Q: 918113.406063 / Q1272;
- live rounded-T ceiling: 919105;
- E-011 measured increment price: `+606.297` executed square T;
- remaining diagnostic allowance: about 385.7 T;
- E-013 static delta over E-011: zero T and no additional coherent qubit;
- repaired miter seed provenance remains frozen as
  `b"e011-call9-boundary-miter"`; it does not claim continuity with E-011's
  removed seed.

Protected arithmetic, product-square, evaluator, and tracked-results hashes
remain the E-012 values until an admitted source edit. The opt-in is
`TLM_SQUARE_C_HALF_BOUNDARY_PHASE_REORDER=1`; protected defaults and every
non-call-9 path must remain operation-byte-identical.

## Frozen gates

1. Run the exact E-012 eight-lane input/order/seed miter. Require all eight
   full 256-bit values exact, phase mask zero, and every non-register qubit
   zero.
2. Run the unchanged 64-lane `product-register-square-inputs` corpus. Require
   exact value, phase, and ancilla for all lanes.
3. Build the full E-007 plus E-013 composition. Require Q1272, protected
   opt-out operation identities, and frozen same-seed diagnostic rounded T at
   most 919105 with no classical, phase, or ancilla regression.

Stop at the first failure. No full 9,024-shot evaluation, H64, scan, hunt,
provider/remote action, spend, submission, adaptive repair, or generated
artifact commit is authorized. Freeze and push either result.

## Terminal result

The sole admitted reorder preserved all eight exact values, every source and
sign input, and a zero dirty mask. The focused circuit remained Q515 with 817
emitted Toffolis. It did not clear the coherent channel: the repaired-seed
phase mask was `0x24`.

Measuring and correcting overflow while the carry ladder remained live, then
temporarily restoring the overflow qubit from that classical result, therefore
does not preserve the ladder's coherent reverse semantics. The miter's first
gate failed. Square64, composition, and Q/T measurement were not run.

The reorder, callback plumbing, and temporary miter harness were removed.
Protected source/results hashes reproduce exactly. Terminal verdict:
`VALUE_EXACT; DIRTY_ZERO; PHASE_KILL; SOURCE_REMOVED`. No adaptive repair,
hunt, provider, spend, remote, or submission action ran.
